PySide6>=6.11
python-mpv>=1.0.8
requests>=2.31
unidecode>=1.3
# Arrives with requests anyway, but mpv is pointed at its bundle explicitly
# now (core/mpvloader.ca_bundle), so the dependency is a direct one.
certifi>=2024.2
