.venv/
__pycache__/
*.py[cod]
vendor/libmpv/*.dll
build/
dist/
*.spec

# Local snapshots of the upstream playlist repositories, used only to
# regenerate the catalogues in resources/. The catalogues are committed; the
# snapshots are not, since Winnotix always fetches playlists live.
IPTVrepo/
newIPTVrepo/
